/**
 * Configuration Exports
 * 
 * Central export point for all configuration files.
 * Import from here to access theme and navigation config.
 */

export { themeConfig, type ThemeConfig } from './theme';
export { navigationConfig, getNavigationItems } from './navigation';
